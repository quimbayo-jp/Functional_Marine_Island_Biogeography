# Packages ------------------------------------------------------------- 
# This code was created to installing and loading all the packages used 
# during the analysis and production of the figures

if(!require(plyr)){install.packages('plyr'); library(plyr)}
if(!require(vegan)){install.packages('vegan'); library(vegan)}
if(!require(FD)){install.packages('FD'); library(FD)}
if(!require(cluster)){install.packages('cluster'); library(cluster)}
if(!require(compare)){install.packages('compare'); library(compare)}
if(!require(reshape)){install.packages('reshape'); library(reshape)}
if(!require(DHARMa)){install.packages('DHARMa'); library(DHARMa)}
if(!require(MuMIn)){install.packages('MuMIn'); library(MuMIn)}
if(!require(dplyr)){install.packages('dplyr'); library(dplyr)}
if(!require(lme4)){install.packages('lme4'); library(lme4)}
if(!require(MASS)){install.packages('MASS'); library(MASS)}
if(!require(car)){install.packages('car'); library(car)}
if(!require(betareg)){install.packages('betareg'); library(betareg)}
if(!require(sjPlot)){install.packages('sjPlot'); library(sjPlot)}
if(!require(ggplot2)){install.packages('ggplot2'); library(ggplot2)}
if(!require(ggpubr)){install.packages('ggpubr'); library(ggpubr)}
if(!require(effects)){install.packages('effects'); library(effects)}
if(!require(rd)){install.packages('effects'); library(effects)}
if(!require(maps)){install.packages('maps'); library(maps)}
if(!require(mapproj)){install.packages('mapproj'); library(mapproj)}
if(!require(GISTools)){install.packages('GISTools'); library(GISTools)}
if(!require(rgdal)){install.packages('rgdal'); library(rgdal)}
if(!require(sjPlot)){install.packages('sjPlot'); library(sjPlot)}
if(!require(lmtest)){install.packages('sjPlot'); library(lmtest)}
if(!require(pgirmess)){install.packages('pgirmess'); library(pgirmess)}
if(!require(ggpmisc)){install.packages('ggpmisc'); library(ggpmisc)}
if(!require(betareg)){install.packages('betareg'); library(betareg)}
